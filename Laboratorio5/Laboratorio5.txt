sqlite3

CREATE TABLE "JOBS"
(
[ID] INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
[NAME] TEXT NOT NULL DEFAULT "TOP SECRET"
);

INSERT INTO JOBS VALUES (0 , "INNOVATION MANAGER");

INSERT INTO JOBS(NAME) VALUES
("INNOVATION CENTER ADMIN"),
("HARDWARE DEVELOPER"),
("SOFTWARE DEVELOPER"),
("ADMINISTRATIVE ASSISTANT"),
("IT SUPPORT TECHNICIAN"),
("REVIEWER"),
("CYBERSEC SPECIALIST")
;


CREATE TABLE "EMPLOYEES"
(
[ID] INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
[NAME] TEXT NOT NULL,
[GENDER] TEXT CHECK( GENDER IN ('F','M','U') ) NOT NULL DEFAULT 'U',
[AGE] FLOAT NOT NULL CHECK (AGE BETWEEN 18 AND 70),
[JOB] INTEGER NOT NULL,
FOREIGN KEY ([JOB]) REFERENCES "JOBS" ( [ID] )
ON DELETE RESTRICT ON UPDATE RESTRICT
);

PRAGMA foreign_keys = ON;

INSERT INTO EMPLOYEES VALUES(0,"FEDERICO CASTRO", "M", 54, 0);

INSERT INTO EMPLOYEES (NAME,JOB,AGE)
VALUES("CARLOS MORALES",3,17);


INSERT INTO EMPLOYEES (JOB,AGE,NAME)
VALUES(7,37,"SERGINA HUTT");

.dump EMPLOYEES

INSERT INTO EMPLOYEES(NAME,GENDER,AGE,JOB) VALUES
("ROBIN GONZALEZ", "M", 24, 1),
("SHINJI RODRIGUEZ", "M", 20, 2),
("JORGE SOTO", "M", 21, 3),
("VALERIA TREJOS", "F", 31, 4),
("JAFET LEDEZMA", "M", 20, 2),
("ALEK CASTILLO", "M", 20, 3),
("ESTEBAN MATA", "M", 20, 6),
("KEVIN CASTRO", "M", 21, 5)
;

UPDATE EMPLOYEES SET GENDER = "F", JOB = 3 WHERE NAME = "SERGINA HUTT";

.dump EMPLOYEES


SELECT JOBS.NAME
FROM EMPLOYEES, JOBS
WHERE EMPLOYEES.JOB=JOBS.ID AND EMPLOYEES.AGE BETWEEN 20 AND 30;

SELECT EMPLOYEES.NAME
FROM EMPLOYEES
WHERE EMPLOYEES.JOB = 3 AND EMPLOYEES.AGE >= 30;

